#!dontrunme
PATH=${ZOO_ROOT}/dash/bin:${ZOO_ROOT}/compilers/mono-2.10.2/out/bin

TMP1=$(mktemp --tmpdir=${ZOO_ROOT}/scratch)
TMP2=$(mktemp --tmpdir=${ZOO_ROOT}/scratch)

cat > ${TMP1}

dmcs "-out:${TMP2}" "${TMP1}"
mono "${TMP2}"

rm "${TMP1}" "${TMP2}"
